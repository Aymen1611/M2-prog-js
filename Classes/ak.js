class Weapon {
   constructor(

    name,
    munition,
    recoil,
    effectiveFiringRange,
    maximumFiringRange,
    FeedSystem,
    Sights,
    MuzzleVelocity,

    ) {

    this.name; =
    this.munition,=
    this.recoil, =
    this.EffectiveFiringRange, =
    this.MaximumFiringRange, =
    this.FeedSystem, =
    this.Sights, =
    this.MuzzleVelocity, =
    }
      shoot(shootingspeed) 

    }